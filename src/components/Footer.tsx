import React from "react";

export const Footer: React.FC = () => {
		return (
			<footer className="w-full bg-muted/50 py-6 text-center text-sm text-muted-foreground">
				<div className="container mx-auto px-4">
					{/* Footer content intentionally left blank — main footer exists elsewhere */}
				</div>
			</footer>
		);
};

export default Footer;
